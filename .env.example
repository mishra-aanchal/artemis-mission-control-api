# Server
PORT=3000
ADMIN_KEY=change-this-to-a-real-secret

# PostgreSQL connection
DATABASE_URL=postgresql://user:password@localhost:5432/artemis_workshop

# OR individual components (DATABASE_URL takes precedence if set)
PGHOST=localhost
PGPORT=5432
PGDATABASE=artemis_workshop
PGUSER=artemis
PGPASSWORD=your-password
